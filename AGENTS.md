# Fate Weaver — AI 구현 규칙

이 저장소에서 AI가 코드를 작성·수정할 때 반드시 지켜야 하는 규칙이다. 게임 규칙은 순수 C#
코어(`FateWeaver.Core`, UnityEngine 미참조)에, 표현·저작은 Unity 레이어에 둔다. 아키텍처 배경은
[`2026-06-18-fate-weaver-core-design.md`](docs/superpowers/specs/2026-06-18-fate-weaver-core-design.md).

**여기엔 명령만 있고 근거·절차는 [`docs/agents/`](docs/agents/)에 있다.** 어떤 도구로 열어도 읽히는
자리다. 훅이 규칙을 막을 때 `Tools/rule-note.sh <번호>`가 그 근거 절을 꺼내 보여준다 — 사본은
문서 하나뿐이므로 `.claude/`에도 훅에도 복사하지 않는다. **번호는 영구 ID다**: 재사용하지 않고,
지운 규칙은 결번으로 남기며, 절 배치와 무관하게 고정이다.

## 명령

처음 한 번 `Tools/setup-dev.sh`로 공유 git 훅을 켠다. 검증은 `Tools/verify.sh` 하나다 — 규칙 검사 +
헤드리스 코어 테스트 + 노트북 테스트를 돌고 실패하면 0이 아닌 코드로 끝난다. **Unity 에디터는
필요 없다.** `--lint`는 기계 검사만(1초 미만), `--quick`은 헤드리스만(약 10초).

게이트는 셋이다: `.githooks/`(커밋 시점, 도구 무관) · CI(push·PR마다, **우회 불가능한 실질 게이트**) ·
Claude Code 훅(더 이른 시점의 미러). 개별 명령·시간·함정은 [`commands.md`](docs/agents/commands.md).

## Unity 레이어

1. **객체를 즉석에서 만들지 않는다.** 프리팹으로 저장하여 재사용한다.
2. **참조와 파일 경로를 하드코딩하지 않는다.** 인스펙터에서 할당하거나 프리팹을 사용한다.
3. **런타임 문자열 탐색을 금지한다.** `GameObject.Find`, `FindObjectOfType`, 태그·레이어 이름 문자열 비교, `Resources.Load("magic/string")` 모두 사용하지 않는다.
4. **`public` 필드 대신 `[SerializeField] private`을 쓴다.** 예외는 값만 담고 로직이 없는 **중첩
   `[Serializable]` DTO**뿐이다(`CardArtCatalog.Entry`). DTO가 규칙 판단이나 상태 변경을 하면 예외가 아니다.
5. **콘텐츠는 코드가 아니라 JSON으로 저작한다.** 원본은 `Assets/StreamingAssets/Content/`이고
   부팅 시 `ContentBootstrap.Load`가 읽는다. **SO 카드 저작 파이프라인은 없다** — 남은
   ScriptableObject 셋은 표현 자원 전용이다. [`content-authoring.md`](docs/agents/content-authoring.md)

## 코어 (규칙 레이어)

6. **`FateWeaver.Core`에서 UnityEngine을 참조하지 않는다.** asmdef가 컴파일로 막지만 우회(리플렉션, 어셈블리 이동)도 금지다.
7. **결정론을 보호한다.** 모든 무작위는 `CombatState`의 시드 RNG를 경유한다. 규칙 로직에 `System.Random` 즉석 생성, `DateTime`, `Guid.NewGuid()` 금지.
8. **튜닝 수치를 하드코딩하지 않는다.** 운명력, 드로우 수, HP 같은 값은 변수/데이터로 둔다.
9. **확장은 레지스트리로 한다.** 새 효과/상태/개입 액션 = 핸들러 1개 + 키 등록. 중앙 switch를 키우지 않는다.
10. **카드 설명을 하드코딩하지 않는다.** 설명은 EffectData에서 컴포저가 자동 생성한다.
11. **코어의 출력은 이벤트 타임라인뿐이다.** UI·연출은 이벤트를 재생하며 코어 내부 상태를 직접 뒤지지 않는다.

## 작업 방식

12. **새 규칙 로직에는 헤드리스 테스트가 필수다.** `Tools/verify.sh`로 Unity 없이 검증 가능해야 한다.
    `Assets/Core`의 C#을 고친 세션은 **Stop 훅이 강제한다** — 실패하면 턴이 끝나지 않는다.
    밸런스 변경은 `ScenarioRunner`·`MultiTurnRunner`의 **Compare**(무조작 vs 조작)로 검증한다.
13. **새 패턴을 발명하기 전에 기존 패턴을 검색한다.** 비슷한 핸들러/상태/조건을 먼저 찾아 그 형태를 따른다.
14. **외부 패키지·에셋을 임의로 추가하지 않는다.** 사전 승인이 필요하다. **이미 설치된 것을 쓰는 일은 여기 해당하지 않는다**(규칙 32).

## 병렬 작업

15. **메인 체크아웃(`/Users/ish/Git/rogue-deck`)의 브랜치를 전환하지 않는다.** 거기엔 사용자의 Unity
    에디터가 떠 있어 전환하면 대량 리임포트가 돈다. **세션이 하나뿐인 날에도 유효하다.** 코드 작업은
    전용 워크트리에서 하고 브랜치 이름은 영어로 짓는다. [`worktrees.md`](docs/agents/worktrees.md)
16. **메인 체크아웃에서는 에디터가 보는 경로를 건드리지 않는다.** `Assets/`·`Packages/`·
    `ProjectSettings/` **밖은** 직접 커밋해도 된다 — 문서·`Tools/`·훅·설정이 전부 여기 든다.
    판정 기준은 파일 종류가 아니라 **에디터가 그 경로를 임포트하느냐**다. `pre-commit`이 강제한다.
17. **Unity 작업은 직접 하되, 눈으로 판단할 것만 사용자에게 맡긴다.** 워크트리에서
    씬·프리팹·ScriptableObject·설정을 직접 저작하고, **관리자 객체**(화면에 안 보이고 위치가
    무의미하며 역할이 로직뿐인 GameObject)는 제약 없이 만들고 배선한다. 사용자 몫은 **보이는 결과가
    기준인 것**뿐이다 — 레이아웃·크기·색·연출, 그리고 Play. **판단이 서지 않으면 손대기 전에 묻는다.**
    배치 검증: [`unity-batch-runs.md`](docs/agents/unity-batch-runs.md) · MCP: [`unity-mcp.md`](docs/agents/unity-mcp.md)
18. **세션을 마칠 때 워킹 트리를 깨끗이 남긴다.** 커밋하거나, 못 하면 stash + 사유 기록. 생성물을
    커밋되지 않은 채 방치하지 않는다. **stash는 저장소가 공유하므로 맨 `git stash`를 쓰지 않는다.**
19. **master 머지는 사용자 승인 후에만 한다.** 머지 전 `Tools/verify.sh` 전체 통과를 확인하고, 끝난 브랜치와 워크트리는 정리한다.
20. **문서 색인을 같은 커밋에서 갱신한다.** 완료된 계획은 `docs/superpowers/.archive/`로 옮기고
    **색인에서 그 행을 지운다.** **`.archive/`는 읽지 않는다** — 폐기된 이력이고 그 안의 `규칙 N`
    인용은 작성 당시 기준이다(규칙 5·17이 실제로 뒤집혔다). [`doc-lifecycle.md`](docs/agents/doc-lifecycle.md)

## 코드베이스 탐색 (graphify)

> 상세는 [`graphify-usage.md`](docs/agents/graphify-usage.md). 여기에는 판단 기준만 남긴다.

21. **그래프는 커밋되지 않는 로컬 산출물이다. 조회 전에 재생성한다.** 커밋해 두면 조용히 썩고,
    **낡은 그래프는 침묵하지 않고 틀린 file:line을 확신 있게 말한다.** `Tools/graph/rebuild-graph.sh`(약 11초·0토큰).
22. **조회는 노드 이름을 알 때만 이득이다. 기본값은 Read·grep이다.** 카드·상태의 **관계 질문**과
    심볼의 **호출자·상속 추적**만 그래프가 이긴다. 파일 위치는 grep이, 파일 하나 이해는 Read가 싸다.
    한두 번 물을 뿐이라면 재생성 11초를 들일 이유가 없다 — **그냥 읽어라.**
23. **`/graphify --update`는 돌리지 않는다.** 회당 25만~40만 토큰인데 회수액은 히트당 ~2,900토큰이다. **스스로 제안하지 않는다.**
24. **`graphify-out/cache/semantic/`은 커밋 대상이다.** 지우면 2.28M 토큰을 다시 태워야 한다.

## Unity 실행 장애

25. **"Unity Licensing Client 연결 상실"과 `Licensing is not yet initialized` 행은 좀비 라이선싱
    클라이언트부터 의심한다.** 라이선스도 Hub 버전도 아니다. [`unity-batch-runs.md`](docs/agents/unity-batch-runs.md)
26. **다른 세션의 Unity 프로세스를 죽이지 않는다.** 죽여도 되는 것은 라이선싱 클라이언트뿐이고, 남의 배치가 막혀 있으면 사용자에게 보고한다.

## 커밋 메시지

27. **커밋 메시지 제목과 본문은 한국어로 쓴다.** 형식은 `타입(범위): 한국어 제목`이고 제목은 현재형
    평서문(`-ㄴ다`)으로 끝낸다. `commit-msg` 훅이 강제한다. [`commit-messages.md`](docs/agents/commit-messages.md)

## 문서 작성

28. **설계·계획 문서는 사람 검수용 `.html`과 세션 인계용 `.md`로 나눈다.** 기준은 `.html`이 "이것만
    보고 승인할 수 있는가", `.md`가 "이것만 읽고 구현할 수 있는가"다. **새 문서를 쓰기 전에 반드시
    [`design-doc-format.md`](docs/agents/design-doc-format.md)를 읽는다.**
29. **개요로 승인받고, 상세로 구현한다.** 실행 근거는 `.md`의 `## 상세`에서만 얻는다. **개요와 상세가
    어긋나면 상세를 따르지 않고 멈추고 묻는다** — 불일치는 문서 오류가 아니라 설계 오류 신호다.

## 책임 분배

30. **조정자는 조정만 한다. 기존 객체에 기능을 얹기 전에 멈춘다.** 컨트롤러·매니저류는 배선과 호출
    순서만 정하고 규칙 판단·표시 변환·입력 해석을 하지 않는다. 기능을 **추가**할 때 그 클래스의 책임을
    '그리고' 없이 한 문장으로 못 쓰면 뺄 후보다. **말없이 쪼개지 말고, 얹기 전에 보고하고 결정을 받는다.**
    이 판정은 문서를 쓸 때가 아니라 **코드를 만질 때마다** 한다. [`responsibility.md`](docs/agents/responsibility.md)

## 사실 주장과 근거

31. **코드베이스·문서에 대한 사실 주장에는 근거 경로를 붙인다. 읽지 않았으면 '추측'이라고 명시한다.**
    반복 확인된 실패 패턴(2026-08-28 사용자 지적): AI가 파일을 확인하지 않은 채 기억으로 그럴듯하게
    답하고, 지적받은 뒤에야 읽고 정정한다. "항상 확인해라"류 지시는 몇 턴 만에 감쇠하므로, 확인을
    지시가 아니라 답의 **형식**으로 강제한다 — 근거 인용이 필수면 읽지 않고는 답을 완성할 수 없다.

    - "X는 이렇게 동작한다", "Y는 Z에 있다" 같은 주장에는 `파일:줄` 또는 문서 경로·절 이름을 붙인다.
      근거를 붙일 수 없으면 그 문장은 완성되지 않은 것이다 — 읽고 나서 쓴다.
    - 읽지 않고 말해야 할 때(방향 제시, 일반론)는 **추측임을 문장에 표시한다**: "추측인데 —",
      "확인 전이지만 —". 표시 없는 주장은 확인된 것으로 간주된다.
    - 대화 중 자신의 앞선 주장이 틀렸을 가능성을 감지하면 사용자가 지적하기 전에 **먼저 확인하고
      정정한다.** 지적받고 인정하는 것보다 스스로 정정하는 쪽이 항상 싸다.
    - 이 규칙은 대화 답변만이 아니라 계획·커밋 메시지·문서에 쓰는 사실 서술에도 적용된다. 인용의
      정확성은 규칙 21의 교훈을 따른다 — 낡은 근거는 침묵하지 않고 틀린 file:line을 확신 있게 말하므로,
      인용 직전에 읽은 것만 근거로 쓴다.

## 표현 레이어의 도구 선택

32. **연출·렌더링은 기존 도구를 먼저 쓴다. 직접 구현이 예외다.** 판별 기준은 **"헤드리스로
    검증하는가"** — `dotnet test`로 단언할 로직이면 규칙 6·7·12를 따르고, 눈으로만 확인되는 것이면
    이 규칙의 대상이다. 경계에 걸치면 **쪼갠다.** **설치돼 있는데 안 쓰고 손으로 만들면 그것이
    위반이다**(DOTween·Timeline·2D Animation·URP 등). 손으로 만들었으면 **왜 안 썼는지를 설계
    문서에 남긴다.** 설치 목록과 경계: [`presentation-tools.md`](docs/agents/presentation-tools.md)

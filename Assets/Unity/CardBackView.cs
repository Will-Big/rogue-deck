using UnityEngine;

namespace FateWeaver.Unity
{
    /// <summary>Marker for the card back shown during the placement flight flip.
    /// The temporary look is a gold frame border around an opaque dark center;
    /// swap this prefab's visuals when the real card-back design lands.</summary>
    public sealed class CardBackView : MonoBehaviour
    {
    }
}

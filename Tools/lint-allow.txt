# Tools/verify.sh 규칙 검사의 알려진 예외.
#
# 형식: <검사 키> <경로 또는 문자열>   # 사유
# 검사 키는 verify.sh의 check 호출과 같다 (R3·R4·R6).
#
# 이 목록은 **부채 목록이다.** 새 위반을 여기에 추가해 통과시키지 않는다 — 항목을 지우는 쪽으로만
# 쓴다. 검사가 처음부터 빨간 상태로 도입되면 아무도 보지 않기 때문에, 도입 시점의 기존 위반만
# 여기 적고 게이트는 초록으로 시작한다 (2026-09-03 도입).
#
# 규칙 4의 중첩 [Serializable] DTO는 여기가 아니라 규칙 자체의 예외이며,
# Tools/lint-public-fields.awk가 문맥으로 가려낸다 (2026-09-04).

R3 Assets/Unity/Scripts/Battle/BattleUiKit.cs   # Resources.Load<TMP_FontAsset>("Fonts/KoreanTMP"). 에셋은 커밋돼 있으나 BattleUiKit이 public static class고 전투 UI가 프리팹 없이 코드로 지어져 인스펙터 참조를 둘 자리가 없다. 경로가 틀리면 예외 없이 TMP 기본 폰트로 떨어져 한글이 깨진다 — 구조를 바꾸기보다 부팅 시 시끄럽게 실패시키는 쪽이 값싸다.
R3 Assets/Unity/Scripts/Cards/PlaytestCardArt.cs  # 상태 아이콘("Status/icon_lock") 경로 저작. 이름과 달리 테스트용이 아니라 CardView·RailCardView가 부르는 실 코드이며(playtest 씬 시절 이름이 남은 것), 카드 앞면 아트가 CardArtCatalog로 빠진 뒤 남은 일은 아이콘 해석뿐이다. lock은 폐기되지만 아이콘 기능은 남아 새 아이콘이 이 구조를 물려받는다 — CardArtCatalog와 같은 모양의 아이콘 카탈로그로 바꾸면 해소되고, 아이콘이 하나뿐인 지금이 가장 싸다.

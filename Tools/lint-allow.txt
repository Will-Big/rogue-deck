# Tools/verify.sh 규칙 검사의 알려진 예외.
#
# 형식: <검사 키> <경로 또는 문자열>   # 사유
# 검사 키는 verify.sh의 check 호출과 같다 (R3·R4·R6).
#
# 이 목록은 **부채 목록이다.** 새 위반을 여기에 추가해 통과시키지 않는다 — 항목을 지우는 쪽으로만
# 쓴다. 검사가 처음부터 빨간 상태로 도입되면 아무도 보지 않기 때문에, 도입 시점의 기존 위반만
# 여기 적고 게이트는 초록으로 시작한다 (2026-09-03 도입).

R3 Assets/Unity/Scripts/Battle/BattleUiKit.cs   # Resources.Load<TMP_FontAsset>("Fonts/KoreanTMP") — 머신별 생성 폰트라 인스펙터 참조가 불가능하다. 폰트 생성 파이프라인을 바꿀 때 함께 해소한다.
R3 Assets/Unity/Scripts/Cards/PlaytestCardArt.cs  # Resources.Load/LoadAll 경로 저작. CardArtCatalog로 옮기면 해소된다.

R4 Assets/Unity/Scripts/Content/CardArtCatalog.cs  # 중첩 [Serializable] 엔트리의 public 필드 2개(Id·Art). [SerializeField] private으로 바꿔도 직렬화 이름이 같아 에셋은 유지되지만, 에셋 재직렬화 검증이 필요해 별도 작업으로 둔다.
